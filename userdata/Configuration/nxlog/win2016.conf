define ROOT C:\Program Files (x86)\nxlog

Moduledir %ROOT%\modules
CacheDir  %ROOT%\data
Pidfile   %ROOT%\data\nxlog.pid
SpoolDir  %ROOT%\data
LogFile   %ROOT%\data\nxlog.log

<Extension syslog>
  Module xm_syslog
</Extension>

<Input eventlog>
  Module im_msvistalog
  # ReadFromLast TRUE
  Query <QueryList>\
          <Query Id="0">\
            <Select Path="Microsoft-Windows-DSC/Operational">*</Select>\
            <Select Path="Application">*[System[Provider[@Name='OpenCloudConfig'] or Provider[@Name='HaltOnIdle'] or Provider[@Name='MaintainSystem']]]</Select>\
            <Select Path="Application">*[System[Level=1 or Level=2 or Level=3]]</Select>\
            <Select Path="Security">*[System[Level=1 or Level=2 or Level=3]]</Select>\
            <Select Path="System">*[System[Level=1 or Level=2 or Level=3 or EventID=1074]]</Select>\
            <Select Path="HardwareEvents">*[System[Level=1 or Level=2 or Level=3]]</Select>\
            <Select Path="Microsoft-Windows-TerminalServices-LocalSessionManager/Operational">*</Select>\
            <Select Path="OpenSSH/Operational">*</Select>\
          </Query>\
        </QueryList>
</Input>

<Input generic_worker_log>
  Module im_file
  File "C:\generic-worker\*.log"
  SavePos TRUE
  ReadFromLast TRUE
  InputType LineBased
</Input>

<Input dsc_run_log>
  Module im_file
  File 'C:\\log\\*dsc-run.log'
  SavePos TRUE
  ReadFromLast TRUE
  InputType LineBased
</Input>

<Input nxlog>
  Module im_file
  File 'C:/Program Files (x86)/nxlog/data/nxlog.log'
  SavePos TRUE
  ReadFromLast TRUE
  InputType LineBased
</Input>

<Processor eventlog_transform>
  Module pm_transformer
  Exec if $raw_event =~ /((assumed-role.taskcluster-level)|(spot termination notice not detected)|(drive z:)|(Framework Failed to launch)|(Failed to schedule Software Protection)|(Explorer is not crashing repeatedy)|(launch time is not more than 24 hours ago)|(generic-worker is not idle more than 5 hours))/ drop();
  Exec $Hostname = hostname_fqdn();
  OutputFormat syslog_rfc5424
</Processor>

<Processor generic_worker_transform>
  Module pm_transformer
  Exec $SourceName = 'generic-worker';
  Exec if $raw_event =~ /((Deleted file - Z)|(No task claimed...)|(Zero tasks returned in Azure XML QueueMessagesList))/ drop();
  Exec $Hostname = hostname_fqdn();
  OutputFormat syslog_rfc5424
</Processor>

<Processor dsc_run_transform>
  Module pm_transformer
  Exec $SourceName = 'dsc-run';
  Exec $Hostname = hostname_fqdn();
  OutputFormat syslog_rfc5424
</Processor>

<Processor nxlog_transform>
  Module pm_transformer
  Exec $SourceName = 'nxlog';
  Exec $Hostname = hostname_fqdn();
  OutputFormat syslog_rfc5424
</Processor>

<Output papertrail>
  Module om_ssl
  Host logs3.papertrailapp.com
  Port 49853
  CAFile %ROOT%\cert\papertrail-bundle.pem
  AllowUntrusted FALSE
</Output>

<Route eventlog_papertrail>
  Path eventlog => eventlog_transform => papertrail
</Route>

<Route generic_worker_log_papertrail>
  Path generic_worker_log => generic_worker_transform => papertrail
</Route>

<Route dsc_run_log_papertrail>
  Path dsc_run_log => dsc_run_transform => papertrail
</Route>

<Route nxlog_papertrail>
  Path nxlog => nxlog_transform => papertrail
</Route>
